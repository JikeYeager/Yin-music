<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ly.yin.mapper.CommentMapper">
<!-- 获取指定歌单的评论 -->
    <select id="songListComment" resultType="com.ly.yin.vo.CommentVO">
        select c.*,u.username,u.avator
        from comment c ,consumer u
        where c.user_id = u.id
          and c.song_list_id = #{songListId}
    </select>
    <!-- 获取指定歌曲的评论 -->
    <select id="songComment" resultType="com.ly.yin.vo.CommentVO">
        select c.*,u.username,u.avator
        from comment c ,consumer u
        where c.user_id = u.id
          and c.song_id = #{songId}
    </select>
</mapper>